do local Players=game:GetService("Players");local UserInputService=game:GetService("UserInputService");local ReplicatedStorage=game:GetService("ReplicatedStorage");local RunService=game:GetService("RunService");local ContextActionService=game:GetService("ContextActionService");local Debris=game:GetService("Debris");local LocalPlayer=Players.LocalPlayer;local Character,Head,Humanoid,RootPart,cam;local function UpdateCharacter() Character=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;Head=Character:WaitForChild("Head",10);Humanoid=Character:WaitForChild("Humanoid",10);RootPart=Character:WaitForChild("HumanoidRootPart",10);cam=workspace.CurrentCamera;end UpdateCharacter();local Rayfield=loadstring(game:HttpGet("https://sirius.menu/rayfield"))();local Window=Rayfield:CreateWindow({Name="OxyHub v5.5",LoadingTitle="OxyHub",LoadingSubtitle="FTAP - Full Protection Edition",ConfigurationSaving={Enabled=true,FolderName="OxyHub",FileName="Config"},KeySystem=false});local Tabs={Player=Window:CreateTab("Player"),Combat=Window:CreateTab("Combat"),Render=Window:CreateTab("Render"),Other=Window:CreateTab("Other")};local Connections={};local GrabFolder=ReplicatedStorage:WaitForChild("GrabEvents");local CreateGrabLine=GrabFolder:FindFirstChild("CreateGrabLine");local ExtendGrabLine=GrabFolder:FindFirstChild("ExtendGrabLine");local DestroyGrabLine=GrabFolder:FindFirstChild("DestroyGrabLine");local SetNetworkOwner=GrabFolder:FindFirstChild("SetNetworkOwner");local EndGrabEarly=GrabFolder:FindFirstChild("EndGrabEarly");local GrabPartsTemplate=game:GetService("ReplicatedFirst"):WaitForChild("GrabParts");local BombEvents=ReplicatedStorage:FindFirstChild("BombEvents");if  not BombEvents then BombEvents=ReplicatedStorage:WaitForChild("BombEvents",10);end local BombReplicator,BombExplode;if BombEvents then BombReplicator=BombEvents:FindFirstChild("BombReplicator");BombExplode=BombEvents:FindFirstChild("BombExplode");end local CharacterEvents=ReplicatedStorage:FindFirstChild("CharacterEvents");if  not CharacterEvents then CharacterEvents=ReplicatedStorage:WaitForChild("CharacterEvents",10);end local RagdollRemote;if CharacterEvents then RagdollRemote=CharacterEvents:FindFirstChild("RagdollRemote");end local PlayerEvents=ReplicatedStorage:FindFirstChild("PlayerEvents");if  not PlayerEvents then PlayerEvents=ReplicatedStorage:WaitForChild("PlayerEvents",10);end local RagdollPlayer;if PlayerEvents then RagdollPlayer=PlayerEvents:FindFirstChild("RagdollPlayer");end local BloobmanEvents=ReplicatedStorage:FindFirstChild("BloobmanEvents") or ReplicatedStorage:FindFirstChild("MountEvents") or ReplicatedStorage:FindFirstChild("PetEvents") ;local BloobmanKick,BloobmanMount,BloobmanDismount;if  not BloobmanEvents then for _,folder in ipairs(ReplicatedStorage:GetDescendants()) do if folder:IsA("Folder") then local n=folder.Name:lower();if (n:find("bloob") or n:find("mount") or n:find("ride")) then BloobmanEvents=folder;break;end end end end if BloobmanEvents then for _,child in ipairs(BloobmanEvents:GetChildren()) do local n=child.Name:lower();if n:find("kick") then BloobmanKick=child;end if (n:find("mount") or n:find("ride") or n:find("sit")) then BloobmanMount=child;end if (n:find("dismount") or n:find("unsit") or n:find("leave")) then BloobmanDismount=child;end end end if  not BloobmanKick then for _,v in ipairs(ReplicatedStorage:GetDescendants()) do if (v:IsA("RemoteEvent") or v:IsA("RemoteFunction")) then local n=v.Name:lower();if (n:find("kick") and (n:find("bloob") or n:find("mount") or n:find("rider"))) then BloobmanKick=v;break;end end end end local function ensureFartherReach() if  not LocalPlayer:FindFirstChild("FartherReach") then local bv=Instance.new("BoolValue");bv.Name="FartherReach";bv.Value=true;bv.Parent=LocalPlayer;end LocalPlayer.FartherReach.Value=true;end ensureFartherReach();local levitationEnabled=false;local levitationDuration=1;local isLevitating=false;local wasGrabbed=false;local grabDetectConns={};local function startLevitation() if isLevitating then return;end local char=LocalPlayer.Character;if  not char then return;end local hrp=char:FindFirstChild("HumanoidRootPart");local hum=char:FindFirstChild("Humanoid");if ( not hrp or  not hum or (hum.Health<=0)) then return;end isLevitating=true;local floatPos=hrp.Position;local bp=Instance.new("BodyPosition");bp.Name="OxyLevitate";bp:SetAttribute("OxyHub",true);bp.MaxForce=Vector3.new(100000,1000000,100000);bp.D=1250;bp.P=15000;bp.Position=floatPos;bp.Parent=hrp;pcall(function() hrp.AssemblyLinearVelocity=Vector3.zero;hrp.AssemblyAngularVelocity=Vector3.zero;end);local bg=Instance.new("BodyGyro");bg.Name="OxyLevitateGyro";bg:SetAttribute("OxyHub",true);bg.MaxTorque=Vector3.new(100000,100000,100000);bg.D=500;bg.P=10000;bg.CFrame=CFrame.new(hrp.Position) * CFrame.Angles(0,math.rad(hrp.Orientation.Y),0) ;bg.Parent=hrp;local glow=Instance.new("PointLight");glow.Name="OxyLevitateGlow";glow.Color=Color3.fromRGB(100,180,255);glow.Brightness=2;glow.Range=15;glow.Parent=hrp;local moveConn;moveConn=RunService.Heartbeat:Connect(function() if ( not isLevitating or  not hrp or  not hrp.Parent) then if moveConn then moveConn:Disconnect();end return;end pcall(function() bp.Position=Vector3.new(hrp.Position.X,floatPos.Y,hrp.Position.Z);end);end);task.delay(levitationDuration,function() isLevitating=false;if moveConn then moveConn:Disconnect();end pcall(function() if (bp and bp.Parent) then for i=1,5 do if (bp and bp.Parent) then bp.MaxForce=bp.MaxForce * 0.4 ;bp.Position=bp.Position-Vector3.new(0,3,0) ;end task.wait(0.06);end if (bp and bp.Parent) then bp:Destroy();end end end);pcall(function() if (bg and bg.Parent) then bg:Destroy();end end);pcall(function() if (glow and glow.Parent) then glow:Destroy();end end);end);end local function isGrabWeld(obj) if  not obj:IsA("WeldConstraint") then return false;end local p0=obj.Part0;local p1=obj.Part1;if ( not p0 or  not p1) then return false;end local char=LocalPlayer.Character;if  not char then return false;end local oneIsOurs=p0:IsDescendantOf(char) or p1:IsDescendantOf(char) ;local oneIsExternal= not p0:IsDescendantOf(char) or  not p1:IsDescendantOf(char) ;return oneIsOurs and oneIsExternal ;end local function setupGrabDetection(char) for _,c in ipairs(grabDetectConns) do pcall(function() c:Disconnect();end);end grabDetectConns={};wasGrabbed=false;local weldConn=char.DescendantAdded:Connect(function(obj) if  not levitationEnabled then return;end if isGrabWeld(obj) then wasGrabbed=true;end end);table.insert(grabDetectConns,weldConn);local weldRemoveConn=char.DescendantRemoving:Connect(function(obj) if  not levitationEnabled then return;end if  not wasGrabbed then return;end if  not obj:IsA("WeldConstraint") then return;end task.defer(function() task.wait(0.05);local hrp=char:FindFirstChild("HumanoidRootPart");if  not hrp then return;end if (hrp.AssemblyLinearVelocity.Magnitude>40) then wasGrabbed=false;startLevitation();else wasGrabbed=false;end end);end);table.insert(grabDetectConns,weldRemoveConn);if EndGrabEarly then local c=EndGrabEarly.OnClientEvent:Connect(function() if ( not levitationEnabled or  not wasGrabbed) then return;end task.defer(function() task.wait(0.05);local hrp=char:FindFirstChild("HumanoidRootPart");if  not hrp then return;end if (hrp.AssemblyLinearVelocity.Magnitude>40) then wasGrabbed=false;startLevitation();else wasGrabbed=false;end end);end);table.insert(grabDetectConns,c);end end local antiSuperFlingEnabled=false;local antiSuperFlingConns={};local playerVelocityHistory={};local SUPER_FLING_SPEED_THRESHOLD=200;local SUPER_FLING_DISTANCE=15;local antiSuperFlingShieldActive=false;local shieldPart=nil;local lastSafePosition=nil;local lastSafeTime=0;local function getAttackerFromVelocity() local myChar=LocalPlayer.Character;if  not myChar then return nil;end local myHRP=myChar:FindFirstChild("HumanoidRootPart");if  not myHRP then return nil;end local myPos=myHRP.Position;local closestAttacker=nil;local closestSpeed=0;for _,plr in ipairs(Players:GetPlayers()) do if ((plr~=LocalPlayer) and plr.Character) then local theirHRP=plr.Character:FindFirstChild("HumanoidRootPart");if theirHRP then local theirPos=theirHRP.Position;local dist=(theirPos-myPos).Magnitude;local theirVel=theirHRP.AssemblyLinearVelocity;local dirToMe=(myPos-theirPos).Unit;local approachSpeed=theirVel:Dot(dirToMe);if ((approachSpeed>SUPER_FLING_SPEED_THRESHOLD) and (dist<80)) then if (approachSpeed>closestSpeed) then closestSpeed=approachSpeed;closestAttacker=plr;end end if ((dist<SUPER_FLING_DISTANCE) and (theirVel.Magnitude>150)) then if (theirVel.Magnitude>closestSpeed) then closestSpeed=theirVel.Magnitude;closestAttacker=plr;end end end end end return closestAttacker,closestSpeed;end local function shieldSelf() if antiSuperFlingShieldActive then return;end antiSuperFlingShieldActive=true;local char=LocalPlayer.Character;if  not char then antiSuperFlingShieldActive=false;return;end local hrp=char:FindFirstChild("HumanoidRootPart");if  not hrp then antiSuperFlingShieldActive=false;return;end local safePos=lastSafePosition or hrp.Position ;pcall(function() hrp.AssemblyLinearVelocity=Vector3.zero;hrp.AssemblyAngularVelocity=Vector3.zero;end);local anchor=Instance.new("BodyPosition");anchor.Name="OxySuperFlingShield";anchor:SetAttribute("OxyHub",true);anchor.MaxForce=Vector3.new(math.huge,math.huge,math.huge);anchor.D=2000;anchor.P=50000;anchor.Position=safePos;anchor.Parent=hrp;local gyro=Instance.new("BodyGyro");gyro.Name="OxySuperFlingGyro";gyro:SetAttribute("OxyHub",true);gyro.MaxTorque=Vector3.new(math.huge,math.huge,math.huge);gyro.D=1000;gyro.P=20000;gyro.CFrame=hrp.CFrame;gyro.Parent=hrp;for _,obj in ipairs(char:GetDescendants()) do if ((obj:IsA("BodyVelocity") or obj:IsA("BodyForce") or obj:IsA("BodyThrust") or obj:IsA("VectorForce") or obj:IsA("LinearVelocity") or obj:IsA("BodyAngularVelocity")) and  not obj:GetAttribute("OxyHub")) then pcall(function() obj:Destroy();end);end end local shieldConn;local shieldStart=tick();shieldConn=RunService.Heartbeat:Connect(function() if ((tick() -shieldStart)>0.5) then if shieldConn then shieldConn:Disconnect();end pcall(function() if (anchor and anchor.Parent) then anchor:Destroy();end end);pcall(function() if (gyro and gyro.Parent) then gyro:Destroy();end end);antiSuperFlingShieldActive=false;return;end pcall(function() hrp.AssemblyLinearVelocity=Vector3.zero;hrp.AssemblyAngularVelocity=Vector3.zero;for _,obj in ipairs(char:GetDescendants()) do if ((obj:IsA("BodyVelocity") or obj:IsA("BodyForce") or obj:IsA("BodyThrust") or obj:IsA("VectorForce") or obj:IsA("LinearVelocity")) and  not obj:GetAttribute("OxyHub")) then pcall(function() obj:Destroy();end);end end end);end);end local function collisionFling(targetPlayer) local myChar=LocalPlayer.Character;if  not myChar then return;end local myHRP=myChar:FindFirstChild("HumanoidRootPart");if  not myHRP then return;end local targetChar=targetPlayer.Character;if  not targetChar then return;end local targetHRP=targetChar:FindFirstChild("HumanoidRootPart");if  not targetHRP then return;end local originalPos=myHRP.CFrame;local originalCanCollide={};for _,part in ipairs(myChar:GetDescendants()) do if part:IsA("BasePart") then originalCanCollide[part]=part.CanCollide;part.CanCollide=true;pcall(function() part.CustomPhysicalProperties=PhysicalProperties.new(100,0.3,0.5,1,1);end);end end task.spawn(function() for i=1,15 do if ( not targetHRP or  not targetHRP.Parent) then break;end if ( not myHRP or  not myHRP.Parent) then break;end local targetPos=targetHRP.Position;local offset=Vector3.new(math.random( -3,3),math.random(0,5),math.random( -3,3));pcall(function() myHRP.CFrame=CFrame.new(targetPos + offset );myHRP.AssemblyLinearVelocity=((targetPos-myHRP.Position).Unit * 500) + Vector3.new(0,300,0) ;end);task.wait(0.03);end task.wait(0.1);pcall(function() if (myHRP and myHRP.Parent) then myHRP.CFrame=originalPos;myHRP.AssemblyLinearVelocity=Vector3.zero;myHRP.AssemblyAngularVelocity=Vector3.zero;end end);for part,cc in pairs(originalCanCollide) do pcall(function() if (part and part.Parent) then part.CanCollide=cc;part.CustomPhysicalProperties=PhysicalProperties.new(0.7,0.3,0.5,1,1);end end);end end);end local function standardFling(targetPlayer) local targetChar=targetPlayer.Character;if  not targetChar then return;end local targetHRP=targetChar:FindFirstChild("HumanoidRootPart");if  not targetHRP then return;end local bv=Instance.new("BodyVelocity");bv.MaxForce=Vector3.new(10000000,10000000,10000000);bv.Velocity=Vector3.new(math.random( -500,500),800,math.random( -500,500));bv.Parent=targetHRP;Debris:AddItem(bv,0.3);local bav=Instance.new("BodyAngularVelocity");bav.MaxTorque=Vector3.new(1000000,1000000,1000000);bav.AngularVelocity=Vector3.new(math.random( -50,50),math.random( -50,50),math.random( -50,50));bav.Parent=targetHRP;Debris:AddItem(bav,0.3);end local function counterAttack(targetPlayer) if ( not targetPlayer or  not targetPlayer.Character) then return;end standardFling(targetPlayer);task.delay(0.1,function() collisionFling(targetPlayer);end);Rayfield:Notify({Title="Counter-Attack!",Content="Flinged "   .. targetPlayer.Name   .. " back!" ,Duration=3});end local function setupAntiSuperFling() for _,c in ipairs(antiSuperFlingConns) do pcall(function() c:Disconnect();end);end antiSuperFlingConns={};if  not antiSuperFlingEnabled then return;end local detectConn=RunService.Heartbeat:Connect(function() if  not antiSuperFlingEnabled then return;end local myChar=LocalPlayer.Character;if  not myChar then return;end local myHRP=myChar:FindFirstChild("HumanoidRootPart");if  not myHRP then return;end if (myHRP.AssemblyLinearVelocity.Magnitude<50) then lastSafePosition=myHRP.Position;lastSafeTime=tick();end local attacker,speed=getAttackerFromVelocity();if attacker then shieldSelf();task.spawn(function() task.wait(0.2);counterAttack(attacker);end);end end);table.insert(antiSuperFlingConns,detectConn);local velConn=RunService.Heartbeat:Connect(function() if  not antiSuperFlingEnabled then return;end local myChar=LocalPlayer.Character;if  not myChar then return;end local myHRP=myChar:FindFirstChild("HumanoidRootPart");if  not myHRP then return;end local vel=myHRP.AssemblyLinearVelocity;if ((vel.Magnitude>300) and  not isLevitating and  not antiSuperFlingShieldActive) then local nearestAttacker=nil;local nearestDist=math.huge;for _,plr in ipairs(Players:GetPlayers()) do if ((plr~=LocalPlayer) and plr.Character) then local theirHRP=plr.Character:FindFirstChild("HumanoidRootPart");if theirHRP then local dist=(theirHRP.Position-myHRP.Position).Magnitude;if ((dist<30) and (dist<nearestDist)) then nearestDist=dist;nearestAttacker=plr;end end end end shieldSelf();if nearestAttacker then task.spawn(function() task.wait(0.3);counterAttack(nearestAttacker);end);end end end);table.insert(antiSuperFlingConns,velConn);local function watchCharForSuperFling(char) local c=char.DescendantAdded:Connect(function(obj) if  not antiSuperFlingEnabled then return;end if  not (obj:IsA("BodyVelocity") or obj:IsA("BodyForce") or obj:IsA("BodyThrust") or obj:IsA("VectorForce") or obj:IsA("LinearVelocity") or obj:IsA("BodyAngularVelocity")) then return;end if obj:GetAttribute("OxyHub") then return;end local isSuperFling=false;if (obj:IsA("BodyVelocity") and (obj.Velocity.Magnitude>200)) then isSuperFling=true;elseif (obj:IsA("BodyAngularVelocity") and (obj.AngularVelocity.Magnitude>30)) then isSuperFling=true;elseif (obj:IsA("VectorForce") and (obj.Force.Magnitude>5000)) then isSuperFling=true;elseif (obj:IsA("BodyForce") and (obj.Force.Magnitude>5000)) then isSuperFling=true;end if isSuperFling then pcall(function() obj:Destroy();end);local hrp=char:FindFirstChild("HumanoidRootPart");if hrp then pcall(function() hrp.AssemblyLinearVelocity=Vector3.zero;hrp.AssemblyAngularVelocity=Vector3.zero;end);end local nearestAttacker=nil;local nearestDist=math.huge;if hrp then for _,plr in ipairs(Players:GetPlayers()) do if ((plr~=LocalPlayer) and plr.Character) then local theirHRP=plr.Character:FindFirstChild("HumanoidRootPart");if theirHRP then local dist=(theirHRP.Position-hrp.Position).Magnitude;if ((dist<50) and (dist<nearestDist)) then nearestDist=dist;nearestAttacker=plr;end end end end end shieldSelf();if nearestAttacker then task.spawn(function() task.wait(0.2);counterAttack(nearestAttacker);end);end end end);table.insert(antiSuperFlingConns,c);end if LocalPlayer.Character then watchCharForSuperFling(LocalPlayer.Character);end local charConn=LocalPlayer.CharacterAdded:Connect(function(char) task.wait(0.5);if antiSuperFlingEnabled then watchCharForSuperFling(char);end end);table.insert(antiSuperFlingConns,charConn);end local antiBloobmanKickEnabled=false;local bloobmanConns={};local bloobmanCounterEnabled=true;local function isOnBloobman() local char=LocalPlayer.Character;if  not char then return false;end local hum=char:FindFirstChild("Humanoid");if (hum and hum.Sit) then return true;end for _,obj in ipairs(char:GetDescendants()) do if (obj:IsA("Weld") or obj:IsA("WeldConstraint")) then local p0=obj.Part0 or (obj:IsA("Weld") and obj.Part0) ;local p1=obj.Part1 or (obj:IsA("Weld") and obj.Part1) ;if (p0 and p1) then local ext=nil;if (p0:IsDescendantOf(char) and  not p1:IsDescendantOf(char)) then ext=p1;end if (p1:IsDescendantOf(char) and  not p0:IsDescendantOf(char)) then ext=p0;end if ext then local model=ext:FindFirstAncestorOfClass("Model");if model then local n=model.Name:lower();if (n:find("bloob") or n:find("mount") or n:find("ride") or n:find("pet")) then return true,model;end end end end end end return false;end local function findKickerPlayer() local myChar=LocalPlayer.Character;if  not myChar then return nil;end local myHRP=myChar:FindFirstChild("HumanoidRootPart");if  not myHRP then return nil;end local nearest=nil;local nearDist=math.huge;for _,plr in ipairs(Players:GetPlayers()) do if ((plr~=LocalPlayer) and plr.Character) then local theirHRP=plr.Character:FindFirstChild("HumanoidRootPart");if theirHRP then local dist=(theirHRP.Position-myHRP.Position).Magnitude;if ((dist<40) and (dist<nearDist)) then nearDist=dist;nearest=plr;end end end end return nearest;end local function setupAntiBloobmanKick() for _,c in ipairs(bloobmanConns) do pcall(function() c:Disconnect();end);end bloobmanConns={};if  not antiBloobmanKickEnabled then return;end if BloobmanKick then if BloobmanKick:IsA("RemoteEvent") then local c=BloobmanKick.OnClientEvent:Connect(function(...) if  not antiBloobmanKickEnabled then return;end local wasOnBloob,bloobModel=isOnBloobman();task.defer(function() local hum=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") ;if hum then pcall(function() hum.Sit=true;end);end if bloobmanCounterEnabled then local kicker=findKickerPlayer();if kicker then task.spawn(function() counterAttack(kicker);end);end end end);Rayfield:Notify({Title="Bloobman Kick Blocked!",Content="Someone tried to kick you!",Duration=3});end);table.insert(bloobmanConns,c);end end local function watchSitState(char) local hum=char:WaitForChild("Humanoid",10);if  not hum then return;end local wasSitting=hum.Sit;local lastSitChange=tick();local sitConn;sitConn=hum:GetPropertyChangedSignal("Sit"):Connect(function() if  not antiBloobmanKickEnabled then return;end local now=tick();if (wasSitting and  not hum.Sit) then local timeSitting=now-lastSitChange ;if (timeSitting>1) then task.defer(function() if bloobmanCounterEnabled then local kicker=findKickerPlayer();if kicker then Rayfield:Notify({Title="Kick Detected!",Content=kicker.Name   .. " kicked you — counter-attacking!" ,Duration=4});counterAttack(kicker);end end end);end end wasSitting=hum.Sit;lastSitChange=now;end);table.insert(bloobmanConns,sitConn);end if LocalPlayer.Character then watchSitState(LocalPlayer.Character);end local charConn=LocalPlayer.CharacterAdded:Connect(function(char) task.wait(1);if antiBloobmanKickEnabled then watchSitState(char);end end);table.insert(bloobmanConns,charConn);pcall(function() if (hookmetamethod and getnamecallmethod) then local oldNamecall;oldNamecall=hookmetamethod(game,"__namecall",newcclosure(function(self,...) local method=getnamecallmethod();if antiBloobmanKickEnabled then if ((method=="FireServer") or (method=="InvokeServer")) then local fullName="";pcall(function() fullName=self:GetFullName():lower();end);if (fullName:find("kick") and (fullName:find("bloob") or fullName:find("mount") or fullName:find("rider"))) then Rayfield:Notify({Title="Kick Blocked!",Content="Blocked kick remote call",Duration=3});if bloobmanCounterEnabled then local kicker=findKickerPlayer();if kicker then task.spawn(function() counterAttack(kicker);end);end end return nil;end end end return oldNamecall(self,...);end));end end);end local ToySpawnRemote=nil;local PALLET_NAME="PalletLightBrown";local spawnCooldown=false;local tabSpawnEnabled=false;local function findToyRemote() ToySpawnRemote=nil;local exactNames={"PlaceToy","SpawnToy","PlaceItem","SpawnItem","ToyPlace","ToySpawn","ItemPlace","ItemSpawn"};for _,name in ipairs(exactNames) do local found=ReplicatedStorage:FindFirstChild(name,true);if (found and (found:IsA("RemoteFunction") or found:IsA("RemoteEvent"))) then ToySpawnRemote=found;return;end end for _,folder in ipairs(ReplicatedStorage:GetChildren()) do if folder:IsA("Folder") then for _,child in ipairs(folder:GetChildren()) do if (child:IsA("RemoteFunction") or child:IsA("RemoteEvent")) then local n=child.Name:lower();if (n:find("place") or n:find("toy")) then ToySpawnRemote=child;return;end end end end end for _,v in ipairs(ReplicatedStorage:GetDescendants()) do if v:IsA("RemoteFunction") then local n=v.Name:lower();if (n:find("spawn") or n:find("place")) then ToySpawnRemote=v;return;end end end end findToyRemote();local function spawnPallet() if spawnCooldown then return;end spawnCooldown=true;local char=LocalPlayer.Character;if  not char then spawnCooldown=false;return;end local hrp=char:FindFirstChild("HumanoidRootPart");if  not hrp then spawnCooldown=false;return;end local hum=char:FindFirstChild("Humanoid");if ( not hum or (hum.Health<=0)) then spawnCooldown=false;return;end local spawnCF=hrp.CFrame * CFrame.new(0,3, -8) ;if  not ToySpawnRemote then findToyRemote();end if  not ToySpawnRemote then Rayfield:Notify({Title="Spawn Error",Content="Spawn remote not found.",Duration=5});spawnCooldown=false;return;end if ToySpawnRemote:IsA("RemoteFunction") then local ok,res=pcall(function() return ToySpawnRemote:InvokeServer(PALLET_NAME,spawnCF);end);if (ok and (type(res)=="string") and res:lower():find("own")) then Rayfield:Notify({Title="Spawn Failed",Content="You don't own '"   .. PALLET_NAME   .. "'." ,Duration=5});end elseif ToySpawnRemote:IsA("RemoteEvent") then pcall(function() ToySpawnRemote:FireServer(PALLET_NAME,spawnCF);end);end task.wait(1.5);spawnCooldown=false;end UserInputService.InputBegan:Connect(function(input,gameProcessed) if gameProcessed then return;end if ((input.KeyCode==Enum.KeyCode.Tab) and tabSpawnEnabled) then task.spawn(spawnPallet);end end);local RANGE=1000;local grabbing=false;local grabbedPart=nil;local grabClone=nil;local distance=20;local oxyGrabActive=false;local deathConn=nil;local function doRaycast() cam=workspace.CurrentCamera;local params=RaycastParams.new();params.FilterType=Enum.RaycastFilterType.Exclude;params.IgnoreWater=true;local filterList={};if LocalPlayer.Character then table.insert(filterList,LocalPlayer.Character);end local plotItems=workspace:FindFirstChild("PlotItems");if plotItems then table.insert(filterList,plotItems);end for _,obj in ipairs(workspace:GetChildren()) do if (obj.Name=="GrabParts") then table.insert(filterList,obj);end end params.FilterDescendantsInstances=filterList;return workspace:Raycast(cam.CFrame.Position,cam.CFrame.LookVector * RANGE ,params);end local function endGrab() if  not grabbing then return;end grabbing=false;pcall(function() ContextActionService:UnbindAction("OxyThrow");ContextActionService:UnbindAction("OxyDrop");ContextActionService:UnbindAction("OxyScroll");RunService:UnbindFromRenderStep("oxyDragLoop");end);if grabClone then pcall(function() grabClone:Destroy();end);grabClone=nil;end if (DestroyGrabLine and grabbedPart) then pcall(function() DestroyGrabLine:FireServer(grabbedPart);end);end grabbedPart=nil;end local function startGrab() if grabbing then endGrab();return;end Character=LocalPlayer.Character;if  not Character then return;end Head=Character:FindFirstChild("Head");Humanoid=Character:FindFirstChild("Humanoid");if ( not Humanoid or (Humanoid.Health<=0)) then return;end cam=workspace.CurrentCamera;local hit=doRaycast();if  not hit then return;end local hitPart=hit.Instance;if hitPart:IsDescendantOf(LocalPlayer.Character) then return;end grabbedPart=hitPart;local hitPos=hit.Position;local cframeOffset=grabbedPart.CFrame:ToObjectSpace(CFrame.new(hitPos));distance=math.clamp((hitPos-cam.CFrame.Position).Magnitude,3,RANGE);grabClone=GrabPartsTemplate:Clone();grabClone.DragPart.Position=(cam.CFrame.LookVector * distance) + cam.CFrame.Position ;grabClone.DragPart.Anchored=true;grabClone.GrabPart.WeldConstraint.Part1=grabbedPart;grabClone.GrabPart.Position=hitPos;grabClone.GrabPart.Anchored=false;grabClone.BeamPart.Anchored=true;pcall(function() local rightArm=Character:FindFirstChild("Right Arm");if rightArm then local grip=rightArm:FindFirstChild("RightGripAttachment");if grip then grabClone.BeamPart.GrabBeam.Attachment0=grip;end end end);pcall(function() local bc=LocalPlayer:FindFirstChild("BeamColor");if bc then for _,v in pairs(grabClone:GetDescendants()) do if v:IsA("BasePart") then v.Color=bc:WaitForChild("BallColorHolder").Value;elseif v:IsA("Beam") then v.Color=bc:WaitForChild("ColorSequenceHolder").Color;end end end end);pcall(function() grabClone.BeamPart.GrabBeam.Texture="rbxassetid://8933355899";end);if  not grabbedPart.Anchored then pcall(function() grabbedPart.AssemblyLinearVelocity=Head.AssemblyLinearVelocity;grabbedPart.AssemblyAngularVelocity=Vector3.zero;end);end grabClone.Parent=workspace;grabbing=true;pcall(function() CreateGrabLine:FireServer(grabbedPart,cframeOffset);end);pcall(function() ExtendGrabLine:FireServer(distance);end);pcall(function() SetNetworkOwner:FireServer(grabbedPart,cam.CFrame);end);pcall(function() grabClone.GrabPart.AttachSound:Play();task.wait(0.05);grabClone.GrabPart.BeamSound:Play();end);RunService:BindToRenderStep("oxyDragLoop",Enum.RenderPriority.First.Value,function() if ( not grabbing or  not grabClone or  not grabClone.Parent) then endGrab();return;end pcall(function() cam=workspace.CurrentCamera;grabClone.DragPart.Position=(cam.CFrame.LookVector * distance) + cam.CFrame.Position ;grabClone.BeamPart.CFrame=CFrame.lookAt(grabClone.GrabPart.Position,grabClone.DragPart.Position,Vector3.new(0,0,1));local mag=(grabClone.GrabPart.Position-grabClone.DragPart.Position).Magnitude * 1.5 ;grabClone.BeamPart.GrabBeam.CurveSize1=mag;grabClone.GrabPart.BeamSound.PlaybackSpeed=(mag/2) + 2.5 ;if ( not grabbedPart or  not grabbedPart:IsDescendantOf(workspace)) then endGrab();end end);end);ContextActionService:BindAction("OxyThrow",function(_,state) if (state~=Enum.UserInputState.Begin) then return;end local throwVel=cam.CFrame.LookVector * 50 ;local part=grabbedPart;endGrab();if (part and  not part.Anchored) then pcall(function() part.Velocity=throwVel;end);end end,false,Enum.KeyCode.Q);ContextActionService:BindAction("OxyDrop",function(_,state) if (state~=Enum.UserInputState.Begin) then return;end endGrab();end,false,Enum.KeyCode.R);ContextActionService:BindAction("OxyScroll",function(_,_,input) if  not grabbing then return;end distance=math.clamp(distance + (input.Position.Z * 2) ,3,RANGE);pcall(function() ExtendGrabLine:FireServer(distance);end);end,false,Enum.UserInputType.MouseWheel);end if EndGrabEarly then EndGrabEarly.OnClientEvent:Connect(function() if grabbing then endGrab();end end);end local function activateOxyGrab() if oxyGrabActive then return;end oxyGrabActive=true;UpdateCharacter();ensureFartherReach();local origGrab=Character:FindFirstChild("GrabbingScript");if origGrab then origGrab.Disabled=true;end if deathConn then deathConn:Disconnect();end deathConn=Humanoid.Died:Connect(function() endGrab();end);ContextActionService:BindAction("OxyGrab",function(_,state) if (state==Enum.UserInputState.Begin) then startGrab();end end,false,Enum.UserInputType.MouseButton1);end local function deactivateOxyGrab() if  not oxyGrabActive then return;end oxyGrabActive=false;endGrab();if deathConn then deathConn:Disconnect();end pcall(function() ContextActionService:UnbindAction("OxyGrab");end);if Character then local origGrab=Character:FindFirstChild("GrabbingScript");if origGrab then origGrab.Disabled=false;end end end LocalPlayer.CharacterAdded:Connect(function(newChar) task.wait(1);Character=newChar;Head=Character:WaitForChild("Head",10);Humanoid=Character:WaitForChild("Humanoid",10);RootPart=Character:WaitForChild("HumanoidRootPart",10);cam=workspace.CurrentCamera;ensureFartherReach();if oxyGrabActive then local origGrab=Character:FindFirstChild("GrabbingScript");if origGrab then origGrab.Disabled=true;end if deathConn then deathConn:Disconnect();end deathConn=Humanoid.Died:Connect(function() endGrab();end);end if levitationEnabled then setupGrabDetection(newChar);end if antiSuperFlingEnabled then setupAntiSuperFling();end if antiBloobmanKickEnabled then setupAntiBloobmanKick();end end);Tabs.Player:CreateSlider({Name="Walk Speed",Range={16,500},Increment=1,CurrentValue=16,Flag="Speed",Callback=function(v) pcall(function() local hum=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") ;if hum then hum.WalkSpeed=v;end end);end});Tabs.Player:CreateSlider({Name="Jump Power",Range={50,500},Increment=1,CurrentValue=50,Flag="Jump",Callback=function(v) pcall(function() local hum=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") ;if hum then hum.JumpPower=v;end end);end});Tabs.Player:CreateToggle({Name="Noclip",CurrentValue=false,Flag="Noclip",Callback=function(v) if Connections.Noclip then Connections.Noclip:Disconnect();end if v then Connections.Noclip=RunService.Stepped:Connect(function() pcall(function() local char=LocalPlayer.Character;if char then for _,part in pairs(char:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide=false;end end end end);end);end end});local flySpeed=60;local flyBody,flyConn,flyKeys=nil,nil,{w=false,a=false,s=false,d=false,space=false,shift=false};Tabs.Player:CreateSlider({Name="Fly Speed",Range={20,300},Increment=10,CurrentValue=60,Flag="FlySpeed",Callback=function(v) flySpeed=v;end});Tabs.Player:CreateToggle({Name="Fly (WASD + Space/Shift)",CurrentValue=false,Flag="Fly",Callback=function(v) pcall(function() if v then local hrp=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") ;if  not hrp then return;end flyBody=Instance.new("BodyVelocity");flyBody.MaxForce=Vector3.new(90000,90000,90000);flyBody.Parent=hrp;flyConn=RunService.Heartbeat:Connect(function() local c=workspace.CurrentCamera;if  not c then return;end local dir=Vector3.new();if flyKeys.w then dir=dir + c.CFrame.LookVector ;end if flyKeys.s then dir=dir-c.CFrame.LookVector ;end if flyKeys.a then dir=dir-c.CFrame.RightVector ;end if flyKeys.d then dir=dir + c.CFrame.RightVector ;end if flyKeys.space then dir=dir + Vector3.yAxis ;end if flyKeys.shift then dir=dir-Vector3.yAxis ;end flyBody.Velocity=((dir.Magnitude>0) and (dir.Unit * flySpeed)) or Vector3.zero ;end);else if flyConn then flyConn:Disconnect();end if flyBody then flyBody:Destroy();end end end);end});UserInputService.InputBegan:Connect(function(input) local ok,key=pcall(function() return input.KeyCode.Name:lower();end);if (ok and (flyKeys[key]~=nil)) then flyKeys[key]=true;end end);UserInputService.InputEnded:Connect(function(input) local ok,key=pcall(function() return input.KeyCode.Name:lower();end);if (ok and (flyKeys[key]~=nil)) then flyKeys[key]=false;end end);Tabs.Player:CreateToggle({Name="Anti Fling / Anti Grab",CurrentValue=false,Flag="AntiFling",Callback=function(v) if Connections.Anti then Connections.Anti:Disconnect();end if v then Connections.Anti=RunService.Stepped:Connect(function() pcall(function() local hrp=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") ;if hrp then hrp.AssemblyLinearVelocity=Vector3.zero;hrp.AssemblyAngularVelocity=Vector3.zero;end end);end);end end});Tabs.Player:CreateToggle({Name="Post-Fling Levitation",CurrentValue=false,Flag="Levitation",Callback=function(v) levitationEnabled=v;if v then local char=LocalPlayer.Character;if char then setupGrabDetection(char);end Rayfield:Notify({Title="Levitation ON",Content="Float for "   .. levitationDuration   .. "s when flung!" ,Duration=4});else isLevitating=false;wasGrabbed=false;for _,c in ipairs(grabDetectConns) do pcall(function() c:Disconnect();end);end grabDetectConns={};pcall(function() local char=LocalPlayer.Character;if char then local hrp=char:FindFirstChild("HumanoidRootPart");if hrp then local bp=hrp:FindFirstChild("OxyLevitate");if bp then bp:Destroy();end local bg=hrp:FindFirstChild("OxyLevitateGyro");if bg then bg:Destroy();end local gl=hrp:FindFirstChild("OxyLevitateGlow");if gl then gl:Destroy();end end end end);Rayfield:Notify({Title="Levitation OFF",Content="Normal physics restored",Duration=3});end end});Tabs.Player:CreateSlider({Name="Levitation Duration (sec)",Range={0.5,5},Increment=0.25,CurrentValue=1,Flag="LevDuration",Callback=function(v) levitationDuration=v;end});Tabs.Combat:CreateSlider({Name="Grab Range",Range={100,3000},Increment=50,CurrentValue=1000,Flag="GrabRange",Callback=function(v) RANGE=v;end});Tabs.Combat:CreateToggle({Name="Extended Grab",CurrentValue=false,Flag="ExtGrab",Callback=function(v) if v then activateOxyGrab();Rayfield:Notify({Title="Extended Grab ON",Content="LMB=grab  Scroll=extend  Q=throw  R=drop",Duration=5});else deactivateOxyGrab();Rayfield:Notify({Title="Extended Grab OFF",Content="Normal grab restored",Duration=3});end end});Tabs.Combat:CreateParagraph({Title="Grab Controls",Content="LMB = Grab / Release\nScroll = Extend / Retract\nQ = Throw\nR = Drop"});Tabs.Combat:CreateToggle({Name="TAB = Spawn Item",CurrentValue=false,Flag="TabSpawn",Callback=function(v) tabSpawnEnabled=v;if v then Rayfield:Notify({Title="TAB Spawn ON",Content="Press TAB to spawn '"   .. PALLET_NAME   .. "'" ,Duration=4});end end});Tabs.Combat:CreateInput({Name="Item Name to Spawn",PlaceholderText="PalletLightBrown",RemoveTextAfterFocusLost=false,Callback=function(text) if (text and (text~="")) then PALLET_NAME=text;Rayfield:Notify({Title="Item Set",Content="Now spawning: "   .. text ,Duration=3});end end});local auraR=60;Tabs.Combat:CreateSlider({Name="Fling Aura Range",Range={20,200},Increment=5,CurrentValue=60,Callback=function(v) auraR=v;end});Tabs.Combat:CreateToggle({Name="Fling Aura",CurrentValue=false,Callback=function(v) if Connections.Aura then Connections.Aura:Disconnect();end if v then Connections.Aura=RunService.Heartbeat:Connect(function() pcall(function() local myHRP=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") ;if  not myHRP then return;end for _,plr in Players:GetPlayers() do if ((plr~=LocalPlayer) and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")) then if ((plr.Character.HumanoidRootPart.Position-myHRP.Position).Magnitude<=auraR) then local bv=Instance.new("BodyVelocity");bv.MaxForce=Vector3.new(500000,500000,500000);bv.Velocity=Vector3.new(math.random( -200,200),250,math.random( -200,200));bv.Parent=plr.Character.HumanoidRootPart;Debris:AddItem(bv,0.25);end end end end);end);end end});Tabs.Combat:CreateButton({Name="Fling Everyone",Callback=function() pcall(function() local myHRP=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") ;if  not myHRP then return;end for _,plr in Players:GetPlayers() do if ((plr~=LocalPlayer) and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")) then local bv=Instance.new("BodyVelocity");bv.MaxForce=Vector3.new(10000000,10000000,10000000);bv.Velocity=(myHRP.CFrame.LookVector * 1200) + Vector3.new(0,600,0) ;bv.Parent=plr.Character.HumanoidRootPart;Debris:AddItem(bv,0.2);end end end);end});Tabs.Combat:CreateToggle({Name="Anti Super Fling + Counter",CurrentValue=false,Flag="AntiSuperFling",Callback=function(v) antiSuperFlingEnabled=v;setupAntiSuperFling();if v then Rayfield:Notify({Title="Anti Super Fling ON",Content="Blocks super fling + counter-attacks the attacker!",Duration=5});else Rayfield:Notify({Title="Anti Super Fling OFF",Content="Protection disabled",Duration=3});end end});Tabs.Combat:CreateSlider({Name="Super Fling Detection Range",Range={10,100},Increment=5,CurrentValue=15,Flag="SFDetectRange",Callback=function(v) SUPER_FLING_DISTANCE=v;end});Tabs.Combat:CreateToggle({Name="Anti Bloobman Kick + Counter",CurrentValue=false,Flag="AntiBloobKick",Callback=function(v) antiBloobmanKickEnabled=v;setupAntiBloobmanKick();if v then Rayfield:Notify({Title="Anti Bloobman Kick ON",Content="Blocks kicks & flings the kicker back (bypasses anti-fling)!",Duration=6});else Rayfield:Notify({Title="Anti Bloobman Kick OFF",Content="Kick protection disabled",Duration=3});end end});Tabs.Combat:CreateToggle({Name="Counter-Attack on Kick",CurrentValue=true,Flag="BloobCounter",Callback=function(v) bloobmanCounterEnabled=v;end});Tabs.Combat:CreateParagraph({Title="Protection Info",Content="Anti Super Fling: Detects fast approach + anomalous velocity, shields you & counter-attacks.\n\nAnti Bloobman Kick: Blocks kick remote, detects forced dismount, flings kicker with collision bypass (works vs anti-fling)."});Tabs.Render:CreateToggle({Name="Player ESP",CurrentValue=false,Callback=function(v) for _,plr in Players:GetPlayers() do if (plr~=LocalPlayer) then pcall(function() local char2=plr.Character;if ( not char2 or  not char2:FindFirstChild("Head")) then return;end local existing=char2.Head:FindFirstChild("OxyESP");if v then if existing then existing:Destroy();end local bg=Instance.new("BillboardGui",char2.Head);bg.Name="OxyESP";bg.Size=UDim2.new(0,160,0,40);bg.StudsOffset=Vector3.new(0,3,0);bg.AlwaysOnTop=true;local lbl=Instance.new("TextLabel",bg);lbl.Size=UDim2.new(1,0,1,0);lbl.BackgroundTransparency=1;lbl.Text=plr.Name;lbl.TextScaled=true;lbl.TextColor3=Color3.new(1,0.3,0.3);lbl.TextStrokeTransparency=0.5;lbl.Font=Enum.Font.GothamBold;elseif existing then existing:Destroy();end end);end end end});Tabs.Other:CreateToggle({Name="Godmode",CurrentValue=false,Callback=function(v) if Connections.God then Connections.God:Disconnect();end pcall(function() local hum=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") ;if  not hum then return;end if v then hum.MaxHealth=math.huge;hum.Health=math.huge;Connections.God=RunService.Heartbeat:Connect(function() pcall(function() local h=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") ;if h then h.Health=math.huge;end end);end);else hum.MaxHealth=100;hum.Health=100;end end);end});local antiExplosionEnabled=false;local antiExplosionConns={};local lastExplosionTime=0;local function cleanBodyMovers(char) for _,obj in ipairs(char:GetDescendants()) do if ((obj:IsA("BodyVelocity") or obj:IsA("BodyForce") or obj:IsA("BodyThrust") or obj:IsA("VectorForce") or obj:IsA("LinearVelocity") or obj:IsA("BodyAngularVelocity")) and  not obj:GetAttribute("OxyHub")) then pcall(function() obj:Destroy();end);end end end local function stabilizeCharacter() local char=LocalPlayer.Character;if  not char then return;end local hrp=char:FindFirstChild("HumanoidRootPart");if hrp then pcall(function() hrp.AssemblyLinearVelocity=Vector3.zero;hrp.AssemblyAngularVelocity=Vector3.zero;end);end local hum=char:FindFirstChild("Humanoid");if hum then pcall(function() hum:ChangeState(Enum.HumanoidStateType.GettingUp);hum:SetStateEnabled(Enum.HumanoidStateType.GettingUp,true);hum.Health=hum.MaxHealth;end);end cleanBodyMovers(char);end Tabs.Other:CreateToggle({Name="Anti-Explosion (Full Bypass)",CurrentValue=false,Flag="AntiExplosion",Callback=function(v) antiExplosionEnabled=v;for _,conn in ipairs(antiExplosionConns) do pcall(function() conn:Disconnect();end);end antiExplosionConns={};if v then if BombExplode then local c=BombExplode.OnClientEvent:Connect(function() lastExplosionTime=tick();stabilizeCharacter();task.delay(0.05,stabilizeCharacter);task.delay(0.15,stabilizeCharacter);task.delay(0.3,stabilizeCharacter);end);table.insert(antiExplosionConns,c);end if RagdollRemote then local c=RagdollRemote.OnClientEvent:Connect(function() if  not antiExplosionEnabled then return;end local hum=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") ;if hum then task.defer(function() pcall(function() hum:ChangeState(Enum.HumanoidStateType.GettingUp);hum:SetStateEnabled(Enum.HumanoidStateType.GettingUp,true);end);end);end end);table.insert(antiExplosionConns,c);end if RagdollPlayer then local c=RagdollPlayer.OnClientEvent:Connect(function(player) if  not antiExplosionEnabled then return;end if ((player==LocalPlayer) or (player==LocalPlayer.Character)) then local hum=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") ;if hum then task.defer(function() pcall(function() hum:ChangeState(Enum.HumanoidStateType.GettingUp);hum:SetStateEnabled(Enum.HumanoidStateType.GettingUp,true);end);end);end end end);table.insert(antiExplosionConns,c);end local c=workspace.DescendantAdded:Connect(function(obj) if ( not obj:IsA("Explosion") or  not antiExplosionEnabled) then return;end lastExplosionTime=tick();pcall(function() obj.BlastPressure=0;obj.DestroyJointRadiusPercent=0;end);obj.Hit:Connect(function(part) local char=LocalPlayer.Character;if (char and part:IsDescendantOf(char)) then stabilizeCharacter();end end);end);table.insert(antiExplosionConns,c);local function watchChar(char) if  not char then return;end local cc=char.DescendantAdded:Connect(function(obj) if  not antiExplosionEnabled then return;end if  not (obj:IsA("BodyVelocity") or obj:IsA("BodyForce") or obj:IsA("BodyThrust") or obj:IsA("VectorForce") or obj:IsA("LinearVelocity")) then return;end if obj:GetAttribute("OxyHub") then return;end local isFromExplosion=(tick() -lastExplosionTime)<2 ;if  not isFromExplosion then if (obj:IsA("BodyVelocity") and (obj.Velocity.Magnitude>50)) then isFromExplosion=true;end if (obj:IsA("VectorForce") and (obj.Force.Magnitude>500)) then isFromExplosion=true;end end if isFromExplosion then pcall(function() obj:Destroy();end);local hrp=char:FindFirstChild("HumanoidRootPart");if hrp then pcall(function() hrp.AssemblyLinearVelocity=Vector3.zero;hrp.AssemblyAngularVelocity=Vector3.zero;end);end end end);table.insert(antiExplosionConns,cc);end if LocalPlayer.Character then watchChar(LocalPlayer.Character);end local charConn=LocalPlayer.CharacterAdded:Connect(function(char) task.wait(0.5);if antiExplosionEnabled then watchChar(char);end end);table.insert(antiExplosionConns,charConn);Rayfield:Notify({Title="Anti-Explosion ON",Content="Optimized bypass active",Duration=5});else Rayfield:Notify({Title="Anti-Explosion OFF",Content="Disabled",Duration=3});end end});Tabs.Other:CreateToggle({Name="Anti-Ragdoll",CurrentValue=false,Flag="AntiRagdoll",Callback=function(v) if Connections.AntiRagdoll then pcall(function() Connections.AntiRagdoll:Disconnect();end);end if Connections.AntiRagdoll2 then pcall(function() Connections.AntiRagdoll2:Disconnect();end);end if v then if RagdollRemote then Connections.AntiRagdoll=RagdollRemote.OnClientEvent:Connect(function() local hum=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") ;if hum then task.defer(function() pcall(function() hum:ChangeState(Enum.HumanoidStateType.GettingUp);hum:SetStateEnabled(Enum.HumanoidStateType.GettingUp,true);end);end);end end);end if RagdollPlayer then Connections.AntiRagdoll2=RagdollPlayer.OnClientEvent:Connect(function(plr) if ((plr==LocalPlayer) or (plr==LocalPlayer.Character)) then local hum=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") ;if hum then task.defer(function() pcall(function() hum:ChangeState(Enum.HumanoidStateType.GettingUp);hum:SetStateEnabled(Enum.HumanoidStateType.GettingUp,true);end);end);end end end);end Rayfield:Notify({Title="Anti-Ragdoll ON",Content="No more ragdoll",Duration=3});else Rayfield:Notify({Title="Anti-Ragdoll OFF",Content="Normal ragdoll restored",Duration=3});end end});local ragdolled=false;Tabs.Other:CreateButton({Name="Toggle Ragdoll",Callback=function() pcall(function() local char=LocalPlayer.Character;if  not char then return;end local hum=char:FindFirstChild("Humanoid");if  not hum then return;end ragdolled= not ragdolled;if ragdolled then hum:ChangeState(Enum.HumanoidStateType.Physics);hum:SetStateEnabled(Enum.HumanoidStateType.GettingUp,false);for _,joint in ipairs(char:GetDescendants()) do if joint:IsA("Motor6D") then local socket=Instance.new("BallSocketConstraint");local a0=Instance.new("Attachment",joint.Part0);local a1=Instance.new("Attachment",joint.Part1);a0.CFrame=joint.C0;a1.CFrame=joint.C1;socket.Attachment0=a0;socket.Attachment1=a1;socket.LimitsEnabled=true;socket.TwistLimitsEnabled=true;socket.Parent=joint.Part0;a0.Name="RagdollAttachment";a1.Name="RagdollAttachment";socket.Name="RagdollSocket";joint.Enabled=false;end end Rayfield:Notify({Title="Ragdoll",Content="ON",Duration=2});else hum:ChangeState(Enum.HumanoidStateType.GettingUp);hum:SetStateEnabled(Enum.HumanoidStateType.GettingUp,true);for _,obj in ipairs(char:GetDescendants()) do if obj:IsA("Motor6D") then obj.Enabled=true;end if ((obj.Name=="RagdollSocket") or (obj.Name=="RagdollAttachment")) then obj:Destroy();end end Rayfield:Notify({Title="Ragdoll",Content="OFF",Duration=2});end end);end});Tabs.Other:CreateButton({Name="Rejoin",Callback=function() game:GetService("TeleportService"):Teleport(game.PlaceId);end});Tabs.Other:CreateButton({Name="Print All Remotes (F9)",Callback=function() print("============ ALL REMOTES ============");for _,v in ipairs(ReplicatedStorage:GetDescendants()) do if (v:IsA("RemoteEvent") or v:IsA("RemoteFunction")) then print(v.ClassName,"|",v.Name,"|",v:GetFullName());end end print("============ SPECIAL REMOTES ============");print("BombExplode:",(BombExplode and BombExplode:GetFullName()) or "NOT FOUND" );print("RagdollRemote:",(RagdollRemote and RagdollRemote:GetFullName()) or "NOT FOUND" );print("RagdollPlayer:",(RagdollPlayer and RagdollPlayer:GetFullName()) or "NOT FOUND" );print("BloobmanKick:",(BloobmanKick and BloobmanKick:GetFullName()) or "NOT FOUND" );print("BloobmanEvents:",(BloobmanEvents and BloobmanEvents:GetFullName()) or "NOT FOUND" );print("=====================================");Rayfield:Notify({Title="Debug",Content="Check F9 console",Duration=4});end});Rayfield:Notify({Title="OxyHub v5.5",Content="Full Protection: Anti Super Fling + Anti Bloobman Kick + Counter-Attack!",Duration=8});print("OxyHub v5.5 loaded — full protection suite"); end
